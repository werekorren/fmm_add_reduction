#ifndef FMM_BRUTE_FORCE_H
#define FMM_BRUTE_FORCE_H
#include "fmm_algorithm_specification.h"

void fmm_brute_force_matrix(fmm_matrix *m, int verbose);
void fmm_brute_force_alg(fmm_alg *alg, int verbose);

#endif
